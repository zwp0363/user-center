spring:
  # DataSource Config
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/zwp1
    username: root
    password: zwp123
  # session失效时间
  session:
    timeout: 86400
